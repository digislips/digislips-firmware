.pio/
build/
.vscode/
*.bin
.claude/
__pycache__/
*.pyc
*.pyo
*.ttf
# ...except the till's bundled brand fonts, which the simulator loads at runtime.
!simulator/assets/fonts/*.ttf

# Runtime log written by the desktop launcher, not a build artifact to track
simulator/launch_error.log

# Old hardware — not in active development
DigiSlip_ESP32/

CH341T_setup_notes.md

# Per-device secrets — never commit
DigiSlip_ONX3248G035/config.h

# Internal — not for public repo
CLAUDE.md
CONTEXT.md
docs/
supabase/
port_configs.md
digislips_configs_all.html
digislips_configs_all.pdf
